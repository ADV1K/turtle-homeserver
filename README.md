# Turtle

## Caddy
```shell
cd caddy
mv example.env .env  # add your secrets
docker volume create caddy-data
docker compose up -d
```

## Immich
```shell
cd immich
mv example.env .env  # add your secrets
docker compose up -d
```
